import {mergeStyleSets} from "@fluentui/react";

export const externalStyles = mergeStyleSets({
    container: {
        display: "flex",
        alignItems: 'center',
        cursor: "pointer"
    },
})